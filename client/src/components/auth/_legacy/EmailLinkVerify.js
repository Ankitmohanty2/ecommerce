/**
 * LEGACY — Firebase email link verify UI (commented out, not used).
 */
/*
import React from "react";
import { sendEmailSignInLink, completeEmailLinkSignIn } from "../../utils/firebaseEmailAuth";

function EmailLinkVerify({ email, source, handleActions, setIsLoginComponent }) {
  // Sent magic link via Firebase sendSignInLinkToEmail
  // User clicked link in inbox, then continued signup/login
  return null;
}
*/

export {};
